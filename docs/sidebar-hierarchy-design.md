# 사이드바 계층 구조 설계 명세서 (실전형)

## 1. 설계 철학

### 1.1 핵심 문제점
현재 사이드바가 **"전역(어디에 있는지)"**과 **"컨텍스트(무엇을 할 수 있는지)"**를 한 덩어리로 섞어서 탐색이 흐트러지는 패턴이 가장 흔합니다.

### 1.2 해결 원칙
- **고정 1단(Primary Nav)**: 어떤 화면에서도 흔들리면 안 되는 "큰 카테고리"
- **동적 컨텍스트 패널(Context Nav)**: "현재 선택된 조직/클라이언트/이벤트"가 있을 때만 나타나는 "바로가기"
- **Workspace Switcher**: 현재 컨텍스트를 항상 명확히 표시
- **공통 섹션 구조**: 모든 역할에서 동일한 섹션 순서 유지 (학습 곡선 최소화)

---

## 2. 사이드바 구조 개요

### 2.1 전체 레이아웃

```
┌─────────────────────────────────────┐
│  [Workspace Switcher]               │ ← 고정 상단
│  Mode / Agency / Client / Event     │
├─────────────────────────────────────┤
│  [Overview]                         │ ← Primary Nav
│  - 대시보드                         │
├─────────────────────────────────────┤
│  [Manage]                           │ ← Primary Nav
│  - 관리 항목들                       │
├─────────────────────────────────────┤
│  [Current Event]                    │ ← Context Nav
│  (이벤트 선택 시에만 표시)           │
│  - 라이브 / 콘솔 / 등록자 / 통계    │
├─────────────────────────────────────┤
│  [Insights]                         │ ← Primary Nav
│  - 리포트 / 통계                     │
├─────────────────────────────────────┤
│  [Settings]                         │ ← Primary Nav
│  - 설정 항목들                       │
└─────────────────────────────────────┘
```

### 2.2 섹션 구조 (모든 역할 공통)

1. **Overview**: 대시보드
2. **Manage**: 관리 기능 (역할별로 내용 다름)
3. **Current Event**: 현재 선택된 이벤트의 서브메뉴 (동적)
4. **Insights**: 리포트/통계
5. **Settings**: 설정

> 섹션 순서는 **항상 동일**하게 유지하여 사용자가 학습한 탐색 방식이 깨지지 않도록 합니다.

---

## 3. Workspace Switcher (필수)

### 3.1 목적
사이드바 최상단에 **현재 컨텍스트를 한 줄로 고정 표시**하여 "지금 내가 어디에 있는지"를 항상 명확히 합니다.

### 3.2 UI 구조

```
┌─────────────────────────────────────┐
│  [Workspace]                        │
│  Mode: [Super Admin ▼]              │
│  Agency: [에이전시 A ▼]             │
│  Client: [클라이언트 B ▼]            │
│  Event: [2025 세미나 ▼]              │
└─────────────────────────────────────┘
```

### 3.3 역할별 표시 규칙

#### 슈퍼 관리자
- **Mode**: `Super Admin` (고정, 전환 불가)
- **Agency**: 드롭다운 (전체 에이전시 목록)
- **Client**: 드롭다운 (선택된 Agency의 클라이언트 목록)
- **Event**: 드롭다운 (선택된 Client의 웨비나 목록, 선택적)

#### 에이전시/클라이언트 멤버 (다중 조직 사용자)
- **Mode**: 워크스페이스 타입 전환 가능
  - `Agency` 탭/그룹: 사용자가 속한 모든 에이전시 목록
  - `Client` 탭/그룹: 사용자가 속한 모든 클라이언트 목록
  - 선택 시 해당 조직의 대시보드로 이동
- **Agency** (Agency 모드일 때):
  - 현재 에이전시 (여러 개일 때만 드롭다운)
  - **Client**: 드롭다운 (소속 클라이언트 목록)
    - 선택 시 "클라이언트 보기" 컨텍스트로 전환
    - `← Agency: ABC로 돌아가기` 버튼 표시
- **Client** (Client 모드일 때):
  - 현재 클라이언트 (여러 개일 때만 드롭다운)
  - **Event**: 드롭다운 (소속 웨비나 목록, 최근 10개 + 검색)

> **모드 전환 정합성**: 한 사용자가 여러 조직에 속할 수 있으므로, 슈퍼 관리자를 제외한 사용자는 Agency/Client 모드를 전환할 수 있습니다. 이는 계정 계층 구조의 "다대다 멤버십"과 일치합니다.

### 3.4 구현 규칙

- **드롭다운 클릭 시**: 검색 가능한 모달/드롭다운 표시
- **선택 시**: 해당 컨텍스트로 자동 이동
- **현재 선택 표시**: 강조 색상 (파란색 배경)
- **접힌 상태**: 현재 선택만 표시 (아이콘 + 텍스트)

---

## 4. 역할별 사이드바 상세 구조

### 4.1 슈퍼 관리자 Sidebar

**목표**: "전체"에서 시작해서 Agency/Client/Event 어디든 빠르게 도달 + 시스템 관리

```
[Workspace]
- Mode: Super Admin (고정)
- Agency: [드롭다운 / 검색]
- Client: [드롭다운 / 검색]
- Event: [드롭다운 / 최근] (선택적)

[Overview]
- 📊 대시보드  (/super/dashboard)

[Manage]
- 🏢 에이전시 관리  (/super/agencies)
- 👥 클라이언트 관리 (/super/clients)
- 🎥 이벤트 관리    (/super/webinars)  ← (신규: 전체 이벤트 인덱스)
- 📋 감사 로그      (/super/audit-logs)
- 👤 사용자 관리    (/super/users)     ← (선택)

[Insights]
- 📈 전사 통계      (/super/statistics)

[Settings]
- ⚙️ 내 계정 설정   (/settings/profile)
```

> **섹션 일관성**: 슈퍼 관리자도 공통 섹션 구조(Overview/Manage/Insights/Settings)를 유지합니다. 감사 로그와 사용자 관리는 `[Manage]` 섹션에 포함됩니다.

#### 슈퍼 관리자에서 트리 구조 처리

**❌ 사이드바에 전체 트리 넣지 않기**
- 에이전시 수가 많아지면 사이드바가 무한 스크롤 지옥이 됨
- 정보 밀도가 너무 높아져서 탐색이 어려워짐

**✅ 대신 "빠른 탐색" 섹션 제공**
- `[Manage]` 아래에 **"빠른 탐색(Explorer)"** 섹션 추가:
  - 최근 본 클라이언트 10개
  - 핀 고정 클라이언트
  - 검색 (Search)
- 전체 트리는 `/super/agencies`나 `/super/clients` 목록 페이지에서 제공

**결론**: 슈퍼는 사이드바를 "시스템 네비게이션" 중심으로 두고, 대량 리스트는 페이지에서 처리

---

### 4.2 에이전시 Sidebar

**목표**: 에이전시 기능(클라이언트 관리/리포트/도메인) + "클라이언트 화면으로 진입"이 명확해야 함

```
[Workspace]
- Mode: Agency
- Agency: [현재 에이전시] (여러 개일 때만 드롭다운)
- Client: [드롭다운] → 선택 시 "클라이언트 보기" 컨텍스트로 전환

[Overview]
- 📊 대시보드 (/agency/[agencyId]/dashboard)

[Manage]
- 👥 클라이언트
  - 📋 클라이언트 목록 (/agency/[agencyId]/clients)
  - ➕ 클라이언트 생성 (/agency/[agencyId]/clients/new)  ← owner/admin만
  - 🔍 빠른 이동: 최근/핀/검색

[Insights]
- 📈 리포트 (/agency/[agencyId]/reports)

[Settings]
- 🌐 도메인 (/agency/[agencyId]/domains)
- ⚙️ 내 계정 설정 (/settings/profile)
```

#### "에이전시 ↔ 클라이언트 보기" 전환 명확화

**클라이언트로 들어갔을 때**:
- 사이드바 최상단에 **"← Agency: ABC로 돌아가기"** 버튼 고정 표시
- "지금은 클라이언트 보기" 라벨을 강하게 표시
- 예: `← Agency: ABC로 돌아가기` + `Client: XYZ (viewer)` 배지

이게 없으면 에이전시 유저가 "내가 지금 에이전시 화면인지/클라이언트 화면인지" 놓치기 쉽습니다.

---

### 4.3 클라이언트 Sidebar ⭐ **핵심**

**목표**: 이벤트(웨비나) 관리가 중심 + "각 이벤트별 등록자/통계"로 쉽게 접근

**핵심 설계**: **이벤트 목록(Tree)**과 **현재 이벤트의 서브메뉴(등록자/통계)**를 분리

```
[Workspace]
- Mode: Client
- Client: [현재 클라이언트] (여러 개일 때만 드롭다운)
- Event: [드롭다운 / 최근] → 선택 시 Current Event 섹션 활성화

[Overview]
- 📊 대시보드 (/client/[clientId]/dashboard)

[Manage]
- 🎥 이벤트(웨비나)
  - 📋 전체 이벤트 (/client/[clientId]/webinars)
  - ➕ 이벤트 생성 (/client/[clientId]/webinars/new)  ← owner/admin/operator만
  - 🔍 빠른 이동: 최근 이벤트 10개 + 검색

[Current Event]  ← ⭐ "event가 선택되었거나 /webinar/[id]/*에 있을 때만 노출"
- 📺 라이브 보기        (/webinar/[id]/live)
- 🎛️ 운영 콘솔          (/webinar/[id]/console)
- 👥 등록자             (/webinar/[id]/registrants)  ⭐ 신규
- 📊 통계               (/webinar/[id]/stats)        ⭐ 신규

[Settings]
- 👥 가입계정관리 (/client/[clientId]/accounts)      ← owner/admin만
- 🎨 브랜딩       (/client/[clientId]/settings/branding) ← owner/admin만
- ⚙️ 내 계정 설정 (/settings/profile)
```

#### 왜 이벤트 목록과 Current Event를 분리해야 하나?

**❌ 초안 방식 (트리 구조)**
```
이벤트 관리
  └─ 웨비나 A
      ├─ 라이브
      ├─ 콘솔
      ├─ 등록자
      └─ 통계
  └─ 웨비나 B
      └─ ...
```

**문제점**:
- 이벤트 수가 50~200개만 넘어가면 사이드바가 "스크롤 지옥"
- 현재 작업 중인 이벤트를 찾기 어려움
- 정보 밀도가 너무 높아져서 탐색이 어려움

**✅ 개선 방식 (분리 구조)**
- 이벤트 목록: 전체 목록 페이지 + 최근/검색 중심
- Current Event: 현재 선택된 이벤트의 서브메뉴만 표시

**장점**:
- 사이드바가 항상 깔끔함
- 현재 작업 중인 이벤트가 명확히 표시됨
- 등록자/통계 접근이 빠름

---

## 5. "현재 이벤트" 자동 감지 규칙

### 5.1 레이아웃 전략 (필수)

**문제**: `/webinar/[id]/*` 경로에서 Current Event 섹션을 보여주려면, 해당 페이지에 사이드바 레이아웃이 적용되어야 합니다.

**해결 방안**: Next.js App Router의 Route Group을 활용하여 관리용 웨비나 페이지를 분리합니다.

#### 추천 구조 1: Route Group 분리 (권장)

```
app/
├── (webinar)/              # 공개/참여자 화면 (사이드바 없음)
│   └── webinar/
│       └── [id]/
│           ├── page.tsx    # 입장 페이지
│           └── live/
│               └── page.tsx # 라이브 시청 (참여자용)
│
└── (admin)/                # 관리 화면 (사이드바 있음)
    └── webinar/
        └── [id]/
            ├── console/
            │   └── page.tsx    # 운영 콘솔
            ├── registrants/
            │   └── page.tsx    # 등록자 관리
            └── stats/
                └── page.tsx    # 통계
```

**장점**:
- URL은 그대로 유지 (`/webinar/[id]/console`)
- Route Group은 URL에 노출되지 않음
- 레이아웃을 역할별로 분리 가능

**구현**:
- `(webinar)` 그룹: 미니멀 레이아웃 (사이드바 없음)
- `(admin)` 그룹: 대시보드 레이아웃 (사이드바 있음)

#### 추천 구조 2: Client 하위로 이동 (대안)

```
app/
└── (client)/
    └── client/
        └── [clientId]/
            └── webinars/
                └── [id]/
                    ├── console/
                    ├── registrants/
                    └── stats/
```

**장점**: 관리 화면이 클라이언트 컨텍스트 안에 명확히 위치

**단점**: 기존 `/webinar/[id]/console` URL과 호환성 고려 필요 (리다이렉트)

> **결론**: Route Group 분리 방식(추천 1)을 권장합니다. URL은 유지하면서 레이아웃만 분리할 수 있어 기존 링크와의 호환성이 좋습니다.

### 5.2 URL 기반 감지

**규칙**: `/webinar/[id]/*` 경로에 진입 시, 사이드바가 자동으로 "현재 이벤트"를 설정

```typescript
// 의사코드
function detectCurrentEvent(pathname: string): string | null {
  // /webinar/[id]/* 패턴 매칭
  const match = pathname.match(/^\/webinar\/([^\/]+)/)
  if (match) {
    return match[1] // webinar ID 또는 slug
  }
  return null
}
```

### 5.3 Workspace Switcher 자동 설정 (보강)

**핵심**: Event ID만 감지하는 것이 아니라, **Client/Agency까지 자동으로 세팅**해야 합니다.

#### 자동 세팅 플로우

1. **URL 진입 시**:
   ```typescript
   // 1. URL에서 webinarId 추출
   const webinarId = detectCurrentEvent(pathname)
   
   // 2. 서버에서 webinar → client_id, agency_id 조회
   const { data: webinar } = await supabase
     .from('webinars')
     .select('id, client_id, clients!inner(agency_id)')
     .eq('id', webinarId)
     .single()
   
   // 3. Workspace Switcher 상태 일괄 업데이트
   setWorkspace({
     currentAgency: { id: webinar.clients.agency_id, name: ... },
     currentClient: { id: webinar.client_id, name: ... },
     currentEvent: { id: webinar.id, title: webinar.title }
   })
   ```

2. **이벤트 목록에서 클릭 시**:
   - 클라이언트 웨비나 목록에서 이벤트 클릭
   - Workspace의 `Event` 자동 선택
   - `[Current Event]` 섹션 활성화

3. **이벤트 드롭다운에서 선택 시**:
   - 사용자가 드롭다운에서 이벤트 선택
   - 해당 이벤트의 client/agency 정보도 함께 로드
   - Workspace 상태 업데이트
   - `[Current Event]` 섹션 활성화

> **중요**: 이렇게 하면 "이벤트 페이지에서 새로고침했는데 Workspace가 비어있음" 같은 어색함이 사라집니다.

### 5.3 Current Event 섹션 표시 조건

```typescript
// Current Event 섹션 표시 조건
const shouldShowCurrentEvent = (
  currentEventId: string | null,
  pathname: string
): boolean => {
  // 1. Workspace에서 Event가 선택되어 있거나
  if (currentEventId) return true
  
  // 2. /webinar/[id]/* 경로에 있으면
  if (pathname.match(/^\/webinar\/[^\/]+/)) return true
  
  return false
}
```

---

## 6. 등록자/통계 페이지 설계

### 6.1 웨비나 등록자 페이지 (`/webinar/[id]/registrants`)

**목적**: 특정 웨비나의 등록자 목록 및 관리

**기능**:
- 등록자 목록 (이메일, 이름, 등록 시간, 등록 출처)
- 등록 출처별 필터링 (이메일/수동/초대)
- 등록자 검색
- 등록자 수 통계
- CSV 내보내기

**권한** (최종 확정):
- ✅ 클라이언트 멤버: **owner, admin, operator** (등록자 목록은 개인정보이므로 제한)
- ✅ 에이전시 멤버: **owner, admin** (소속 클라이언트의 웨비나만)
- ✅ 슈퍼 관리자: 모든 웨비나 접근 가능
- ❌ **member, analyst**: 등록자 목록 접근 불가 (개인정보 보호)

**라우팅**:
- `/webinar/[id]/registrants` (웨비나 ID 또는 slug)
- Route Group: `(admin)` (사이드바 레이아웃 적용)

> **권한 정책**: 등록자 목록은 이메일/이름 등 개인정보를 포함하므로, 운영 콘솔 접근 가능한 `member`라도 등록자 목록은 `owner/admin/operator`만 접근 가능합니다.

---

### 6.2 웨비나 통계 페이지 (`/webinar/[id]/stats`)

**목적**: 특정 웨비나의 상세 통계

**기능**:
1. **개요 카드**
   - 총 등록자 수
   - 실제 접속자 수 (최대 동시 접속자)
   - 활성 참여자 비율

2. **상호작용 통계**
   - 총 메시지 수
   - 총 질문 수
   - 답변률
   - 평균 답변 시간

3. **폼/퀴즈 통계**
   - 설문 응답률
   - 퀴즈 평균 점수
   - 퀴즈 정답률

4. **시간대별 차트**
   - 접속자 수 추이
   - 메시지/질문 수 추이

5. **등록 출처 파이 차트**
   - 이메일/수동/초대 비율

**권한** (최종 확정):
- ✅ 클라이언트 멤버: **owner, admin, operator, analyst, member** (통계는 집계 데이터이므로 읽기 허용)
- ✅ 에이전시 멤버: **owner, admin, analyst** (소속 클라이언트의 웨비나만)
- ✅ 슈퍼 관리자: 모든 웨비나 접근 가능

**라우팅**:
- `/webinar/[id]/stats` (웨비나 ID 또는 slug)
- Route Group: `(admin)` (사이드바 레이아웃 적용)

> **권한 정책**: 통계는 집계 데이터이므로 개인정보가 노출되지 않습니다. 따라서 `member`와 `analyst`도 읽기 접근이 가능합니다.

---

## 7. 권한 기반 메뉴 표시 규칙

### 7.1 표시 규칙

| 권한 | 표시 방식 | 예시 |
|------|----------|------|
| **접근 불가** | 아예 숨김 | Analyst의 "이벤트 생성" |
| **조회만 가능** | 표시하되 disabled 또는 읽기 전용 배지 | Analyst의 "통계" (보이되 수정 불가) |
| **수정 가능** | 정상 표시 | Owner/Admin의 "이벤트 생성" |

### 7.2 역할별 메뉴 표시 (최종 권한 매트릭스)

#### 클라이언트 멤버

| 메뉴 | Owner | Admin | Operator | Analyst | Member |
|------|-------|-------|----------|---------|--------|
| 대시보드 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 이벤트 목록 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 이벤트 생성 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 운영 콘솔 | ✅ | ✅ | ✅ | ❌ | ✅ |
| 등록자 | ✅ | ✅ | ✅ | ❌ | ❌ |
| 통계 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 가입계정관리 | ✅ | ✅ | ❌ | ❌ | ❌ |
| 브랜딩 | ✅ | ✅ | ❌ | ❌ | ❌ |

#### 에이전시 멤버 (클라이언트 보기 모드)

| 메뉴 | Owner | Admin | Analyst | Viewer |
|------|-------|-------|---------|--------|
| 클라이언트 대시보드 | ✅ | ✅ | ❌ | ✅ |
| 이벤트 목록 | ✅ | ✅ | ❌ | ✅ |
| 운영 콘솔 | ✅ | ✅ | ❌ | ❌ |
| 등록자 | ✅ | ✅ | ❌ | ❌ |
| 통계 | ✅ | ✅ | ✅ | ❌ |
| 리포트 | ✅ | ✅ | ✅ | ❌ |
| 설정 메뉴 | ❌ | ❌ | ❌ | ❌ |

> **권한 정책 요약**:
> - **등록자**: 개인정보 포함 → owner/admin/operator만 (member 제외)
> - **통계**: 집계 데이터 → member/analyst도 읽기 가능
> - **운영 콘솔**: member는 접근 가능하나 등록자 목록은 제한

---

## 8. UI/UX 상세 명세

### 8.1 섹션 스타일

**섹션 헤더**:
```css
- 패딩: 12px 24px
- 폰트: 12px, font-semibold, uppercase, letter-spacing
- 색상: text-gray-400
- 배경: transparent
```

**메뉴 아이템 (1단계)**:
```css
- 패딩: 12px 24px
- 폰트: 14px, font-medium
- 아이콘: 18px
- 높이: 44px
```

**메뉴 아이템 (2단계 - 하위 메뉴)**:
```css
- 패딩: 10px 24px 10px 48px
- 폰트: 13px, font-normal
- 아이콘: 16px
- 높이: 40px
```

### 8.2 상태 표시

**활성 상태** (현재 경로):
- 배경: `bg-blue-600`
- 텍스트: `text-white`
- 왼쪽 테두리: `border-l-4 border-blue-400`

**호버 상태**:
- 배경: `bg-gray-700` (다크 모드) / `bg-gray-100` (라이트 모드)

**Current Event 섹션 활성화**:
- 섹션 헤더 배경: `bg-blue-900/30`
- 하위 메뉴 활성화: 더 얕은 강조 (톤 다운)

### 8.3 Workspace Switcher 스타일

```css
- 배경: bg-gray-800
- 패딩: 16px
- 테두리: border-b border-gray-700
- 드롭다운 버튼: 
  - 패딩: 8px 12px
  - 호버: bg-gray-700
  - 활성: bg-blue-600
```

---

## 9. 데이터 구조 및 API 설계

### 9.1 사이드바 데이터 구조

```typescript
interface SidebarData {
  // Workspace 정보
  workspace: {
    mode: 'super' | 'agency' | 'client'
    currentAgency?: { id: string; name: string }
    currentClient?: { id: string; name: string }
    currentEvent?: { id: string; title: string }
  }
  
  // Primary Nav (고정)
  primaryNav: {
    overview: NavItem[]
    manage: NavItem[]
    insights: NavItem[]
    settings: NavItem[]
  }
  
  // Context Nav (동적) - 클라이언트에서 정적 생성
  // 서버는 eventId만 제공, 메뉴 아이템은 클라이언트에서 생성
  
  // Explorer (빠른 탐색)
  explorer?: {
    recent: Array<{ id: string; name: string; type: 'client' | 'event' }>
    pinned: Array<{ id: string; name: string; type: 'client' | 'event' }>
  }
}

interface NavItem {
  id: string
  label: string
  icon: string
  href: string
  badge?: number
  disabled?: boolean
  hidden?: boolean
  children?: NavItem[]  // 하위 메뉴 (2단계까지만)
}
```

### 9.2 API 엔드포인트

#### 슈퍼 관리자
**엔드포인트**: `GET /api/super/sidebar-data`

**응답**:
```json
{
  "workspace": {
    "mode": "super",
    "agencies": [
      { "id": "uuid", "name": "에이전시 A" }
    ],
    "clients": [
      { "id": "uuid", "name": "클라이언트 1", "agencyId": "uuid" }
    ]
  },
  "primaryNav": { ... },
  "explorer": {
    "recent": [
      { "id": "uuid", "name": "클라이언트 1", "type": "client" }
    ]
  }
}
```

#### 에이전시
**엔드포인트**: `GET /api/agency/[agencyId]/sidebar-data`

**응답**:
```json
{
  "workspace": {
    "mode": "agency",
    "currentAgency": { "id": "uuid", "name": "에이전시 A" },
    "clients": [
      { "id": "uuid", "name": "클라이언트 A" }
    ]
  },
  "primaryNav": { ... }
}
```

#### 클라이언트
**엔드포인트**: `GET /api/client/[clientId]/sidebar-data`

**응답**:
```json
{
  "workspace": {
    "mode": "client",
    "currentClient": { "id": "uuid", "name": "클라이언트 A" },
    "events": [
      { 
        "id": "uuid", 
        "title": "웨비나 제목", 
        "slug": "webinar-slug",
        "isLive": false,
        "newRegistrants": 3,
        "totalRegistrants": 150
      }
    ]
  },
  "primaryNav": { ... }
}
```

> **API 일관성**: Current Event의 메뉴 아이템(live/console/registrants/stats)은 **클라이언트에서 정적 생성**합니다. 서버는 eventId와 메타데이터만 제공하며, 메뉴 구조는 클라이언트에서 `eventId`를 바인딩하여 생성합니다. 이렇게 하면 API가 단순해지고 사이드바 렌더링도 예측 가능합니다.

---

## 10. 구현 단계

### Phase 1: Workspace Switcher 구현 (필수)
- [ ] Workspace Switcher 컴포넌트 생성
- [ ] 역할별 드롭다운 구현
- [ ] 현재 컨텍스트 표시
- [ ] 컨텍스트 전환 로직

### Phase 2: Primary Nav 구조화
- [ ] 섹션 구조 통일 (Overview/Manage/Insights/Settings)
- [ ] 역할별 메뉴 구성
- [ ] 권한 기반 메뉴 필터링

### Phase 3: Current Event 섹션 (핵심)
- [ ] URL 기반 이벤트 감지
- [ ] Current Event 섹션 동적 표시
- [ ] 등록자/통계 페이지 라우팅

### Phase 4: 신규 페이지 구현
- [ ] `/webinar/[id]/registrants` - 등록자 페이지
- [ ] `/webinar/[id]/stats` - 통계 페이지
- [ ] `/client/[clientId]/reports` - 클라이언트 리포트 (선택)
- [ ] `/super/statistics` - 전사 통계 (선택)

### Phase 5: Explorer (빠른 탐색)
- [ ] 최근 본 항목 추적
- [ ] 핀 고정 기능
- [ ] 검색 기능

---

## 11. 바로 적용 가능한 "최소 변경" 버전

현재 사이드바를 크게 뜯지 않고도 체계화하려면:

### (1) 사이드바 상단에 Workspace Switcher 추가
- "대행사 / 클라이언트 / 이벤트" 선택이 항상 보이게
- 현재 선택된 컨텍스트 강조

### (2) 클라이언트 사이드바에서 "웨비나별 하위 메뉴"를 트리에서 빼고 Current Event 섹션으로 이동
- `/webinar/[id]/*` 경로 진입 시 Current Event 섹션 자동 표시
- 등록자/통계 메뉴를 Current Event 하위로 이동

### (3) 웨비나 목록 트리는 "최근 10개 + 검색"으로 제한
- 전체 목록은 `/client/[clientId]/webinars` 페이지에서 처리
- 사이드바에는 최근/핀/검색만

### (4) 슈퍼 관리자에서 '에이전시 트리 + 클라이언트 메뉴' 중복을 줄이고, Manage를 인덱스 페이지 중심으로
- 사이드바에 전체 트리 넣지 말고, Explorer 섹션으로 최근/핀만
- 전체 목록은 각 관리 페이지에서 처리

---

## 12. 현재 이벤트 감지 및 Workspace 자동 세팅 의사코드

```typescript
// components/layout/Sidebar.tsx

function useCurrentEvent() {
  const pathname = usePathname()
  const params = useParams()
  const [workspace, setWorkspace] = useState<WorkspaceState>({
    currentAgency: null,
    currentClient: null,
    currentEvent: null
  })
  
  // 1. URL에서 이벤트 ID 추출
  const eventIdFromUrl = (() => {
    // /webinar/[id]/* 패턴
    const match = pathname.match(/^\/webinar\/([^\/]+)/)
    if (match) return match[1]
    
    // /client/[clientId]/webinars/[id] 패턴 (선택적)
    const clientMatch = pathname.match(/^\/client\/[^\/]+\/webinars\/([^\/]+)/)
    if (clientMatch) return clientMatch[1]
    
    return null
  })()
  
  // 2. Workspace Switcher에서 선택된 이벤트
  const [selectedEventId, setSelectedEventId] = useState<string | null>(null)
  
  // 3. 최종 현재 이벤트 결정
  const currentEventId = eventIdFromUrl || selectedEventId
  
  // 4. URL 진입 시 Workspace 자동 세팅 (보강)
  useEffect(() => {
    if (eventIdFromUrl) {
      // 서버에서 webinar → client_id, agency_id 조회
      fetch(`/api/webinars/${eventIdFromUrl}/workspace-info`)
        .then(res => res.json())
        .then(data => {
          if (data.webinar) {
            setWorkspace({
              currentAgency: data.agency,
              currentClient: data.client,
              currentEvent: {
                id: data.webinar.id,
                title: data.webinar.title
              }
            })
            setSelectedEventId(eventIdFromUrl)
          }
        })
        .catch(err => console.error('Workspace 정보 조회 실패:', err))
    }
  }, [eventIdFromUrl])
  
  // 5. Current Event 메뉴 아이템 생성 (클라이언트에서 정적 생성)
  const currentEventMenuItems = currentEventId ? [
    { id: 'live', label: '라이브 보기', icon: '📺', href: `/webinar/${currentEventId}/live` },
    { id: 'console', label: '운영 콘솔', icon: '🎛️', href: `/webinar/${currentEventId}/console` },
    { id: 'registrants', label: '등록자', icon: '👥', href: `/webinar/${currentEventId}/registrants` },
    { id: 'stats', label: '통계', icon: '📊', href: `/webinar/${currentEventId}/stats` }
  ] : []
  
  return {
    currentEventId,
    setSelectedEventId,
    isCurrentEventPage: !!eventIdFromUrl,
    workspace,
    setWorkspace,
    currentEventMenuItems
  }
}

// Current Event 섹션 표시 조건
function shouldShowCurrentEventSection(
  currentEventId: string | null,
  pathname: string
): boolean {
  // 1. Workspace에서 Event가 선택되어 있거나
  if (currentEventId) return true
  
  // 2. /webinar/[id]/* 경로에 있으면 (단, 관리 화면만)
  if (pathname.match(/^\/webinar\/[^\/]+\/(console|registrants|stats)/)) return true
  
  return false
}
```

### 12.1 Workspace 정보 조회 API

**엔드포인트**: `GET /api/webinars/[webinarId]/workspace-info`

**응답**:
```json
{
  "webinar": {
    "id": "uuid",
    "title": "웨비나 제목",
    "slug": "webinar-slug"
  },
  "client": {
    "id": "uuid",
    "name": "클라이언트 A"
  },
  "agency": {
    "id": "uuid",
    "name": "에이전시 A"
  }
}
```

---

## 13. 기술 스택

- **컴포넌트**: React (Client Component)
- **상태 관리**: React Context (`SidebarContext`)
- **스타일링**: Tailwind CSS
- **아이콘**: 이모지 (현재 사용 중) 또는 Lucide React (선택)
- **애니메이션**: CSS Transition (300ms)
- **라우팅**: Next.js App Router (`usePathname`, `useParams`)

---

## 14. 참고사항

- **트리 구조는 최대 2단계까지** (1단계: 메뉴, 2단계: 하위 메뉴)
- **접힌 상태에서는 아이콘만 표시**
- **현재 경로에 해당하는 섹션은 자동으로 확장**
- **Workspace Switcher 선택 상태는 localStorage에 저장** (선택적)
- **Current Event 메뉴 아이템은 클라이언트에서 정적 생성** (서버는 eventId와 메타데이터만 제공)
- **Route Group 분리**: 공개 화면 `(webinar)`과 관리 화면 `(admin)`을 분리하여 레이아웃 적용

---

## 15. 예시 화면

### 15.1 클라이언트 사이드바 (이벤트 선택 전)

```
┌─────────────────────────────────────┐
│  [Workspace]                        │
│  Mode: Client                       │
│  Client: 클라이언트 A               │
│  Event: [선택하세요 ▼]              │
├─────────────────────────────────────┤
│  [Overview]                         │
│  📊 대시보드                        │
├─────────────────────────────────────┤
│  [Manage]                           │
│  🎥 이벤트(웨비나)                  │
│    📋 전체 이벤트                   │
│    ➕ 이벤트 생성                   │
│    🔍 최근 이벤트 10개              │
├─────────────────────────────────────┤
│  [Settings]                         │
│  👥 가입계정관리                    │
│  🎨 브랜딩                          │
│  ⚙️ 내 계정 설정                   │
└─────────────────────────────────────┘
```

### 15.2 클라이언트 사이드바 (이벤트 선택 후)

```
┌─────────────────────────────────────┐
│  [Workspace]                        │
│  Mode: Client                       │
│  Client: 클라이언트 A               │
│  Event: 2025 세미나 [▼]             │ ← 선택됨
├─────────────────────────────────────┤
│  [Overview]                         │
│  📊 대시보드                        │
├─────────────────────────────────────┤
│  [Manage]                           │
│  🎥 이벤트(웨비나)                  │
│    📋 전체 이벤트                   │
│    ➕ 이벤트 생성                   │
│    🔍 최근 이벤트 10개              │
├─────────────────────────────────────┤
│  [Current Event]                    │ ← 자동 표시
│  📺 라이브 보기                     │
│  🎛️ 운영 콘솔                      │
│  👥 등록자                          │
│  📊 통계                            │
├─────────────────────────────────────┤
│  [Settings]                         │
│  👥 가입계정관리                    │
│  🎨 브랜딩                          │
│  ⚙️ 내 계정 설정                   │
└─────────────────────────────────────┘
```

---

## 16. 구현 우선순위

### 우선순위 1 (필수)
1. ✅ Workspace Switcher 구현
2. ✅ Current Event 섹션 구현
3. ✅ 웨비나 등록자 페이지 (`/webinar/[id]/registrants`)
4. ✅ 웨비나 통계 페이지 (`/webinar/[id]/stats`)

### 우선순위 2 (중요)
5. Primary Nav 섹션 구조화
6. 에이전시 "클라이언트 보기" 전환 명확화
7. Explorer (빠른 탐색) 구현

### 우선순위 3 (선택)
8. 클라이언트 리포트 페이지
9. 슈퍼 관리자 전사 통계 페이지
10. 핀 고정 기능



