# 도메인 차단 모니터링 가이드

**목적**: `eventflow.kr` 도메인이 다양한 보안 솔루션/네트워크에서 차단되었는지 확인하는 방법

---

## 🔍 1. 도메인 평판 체크 도구

### 무료 온라인 도구

#### 1.1 VirusTotal
- **URL**: https://www.virustotal.com/gui/domain/eventflow.kr
- **기능**: 
  - 70개 이상의 보안 벤더 스캔 결과 확인
  - 도메인 평판 점수
  - 최근 스캔 이력
- **확인 항목**: 악성 사이트로 분류되었는지 확인

#### 1.2 URLVoid
- **URL**: https://www.urlvoid.com/scan/eventflow.kr
- **기능**:
  - 30개 이상의 블랙리스트 체크
  - 도메인 평판 점수
  - IP 주소 정보
- **확인 항목**: 블랙리스트 등재 여부

#### 1.3 Sucuri SiteCheck
- **URL**: https://sitecheck.sucuri.net/
- **기능**:
  - 보안 취약점 스캔
  - 블랙리스트 확인
  - 웹사이트 상태 확인
- **확인 항목**: 보안 이슈 및 블랙리스트 상태

#### 1.4 Google Safe Browsing
- **URL**: https://transparencyreport.google.com/safe-browsing/search?url=eventflow.kr
- **기능**:
  - Google의 안전 브라우징 데이터베이스 확인
  - 피싱/악성 사이트 분류 여부
- **확인 항목**: Google에서 차단 여부

#### 1.5 AbuseIPDB
- **URL**: https://www.abuseipdb.com/check/[IP주소]
- **기능**:
  - IP 주소 평판 확인
  - 악성 활동 보고 이력
- **확인 항목**: IP 기반 차단 여부 (Vercel IP 확인 필요)

---

## 🌐 2. DNS 및 네트워크 확인

### 2.1 DNS 확인
```bash
# Windows PowerShell
nslookup eventflow.kr
Resolve-DnsName eventflow.kr

# 또는 온라인 도구
# https://dnschecker.org/#A/eventflow.kr
```

### 2.2 전 세계 DNS 확인
- **DNS Checker**: https://dnschecker.org/#A/eventflow.kr
- **기능**: 전 세계 여러 지역에서 DNS 해석 확인
- **확인 항목**: 특정 지역에서 DNS 차단 여부

### 2.3 SSL/TLS 인증서 확인
```bash
# PowerShell
$tcpClient = New-Object System.Net.Sockets.TcpClient
$tcpClient.Connect("eventflow.kr", 443)
$sslStream = New-Object System.Net.Security.SslStream($tcpClient.GetStream())
$sslStream.AuthenticateAsClient("eventflow.kr")
$cert = $sslStream.RemoteCertificate
Write-Host $cert.Subject
```

**온라인 도구**:
- **SSL Labs**: https://www.ssllabs.com/ssltest/analyze.html?d=eventflow.kr
- **SSL Checker**: https://www.sslshopper.com/ssl-checker.html#hostname=eventflow.kr

---

## 🛡️ 3. 주요 보안 벤더별 확인

### 3.1 Zscaler (현재 차단된 벤더)
- **확인 방법**: 사내 네트워크에서 접속 시도
- **차단 페이지**: "Newly Registered and Observed Domains" 메시지 확인
- **해결**: IT 팀에 Allowlist 요청 (이미 진행 중)

### 3.2 Palo Alto Networks
- **확인 방법**: 
  - 사내 네트워크에서 접속 시도
  - 또는 Palo Alto 사용 고객사에 문의
- **확인 도구**: 직접 확인 어려움 (사내 네트워크 필요)

### 3.3 Cisco Umbrella (OpenDNS)
- **확인 방법**: 
  - https://umbrella.cisco.com/ (계정 필요)
  - 또는 OpenDNS 사용 네트워크에서 접속 시도
- **확인 도구**: 직접 확인 어려움 (사내 네트워크 필요)

### 3.4 FortiGuard
- **확인 방법**: 
  - https://www.fortiguard.com/webfilter (일부 정보 제공)
  - 또는 Fortinet 사용 네트워크에서 접속 시도

### 3.5 McAfee WebAdvisor
- **확인 방법**: 
  - https://www.siteadvisor.com/ (일부 정보 제공)
  - 또는 McAfee 사용 네트워크에서 접속 시도

---

## 📊 4. 종합 모니터링 서비스

### 4.1 MXToolbox
- **URL**: https://mxtoolbox.com/SuperTool.aspx?action=blacklist%3aeventflow.kr
- **기능**:
  - 다중 블랙리스트 체크
  - DNS 확인
  - 메일 서버 확인
- **확인 항목**: 종합적인 블랙리스트 상태

### 4.2 WhatIsMyIPAddress Blacklist Check
- **URL**: https://whatismyipaddress.com/blacklist-check
- **기능**: IP 주소 기반 블랙리스트 확인
- **주의**: Vercel은 동적 IP이므로 확인이 제한적

### 4.3 BlacklistAlert
- **URL**: https://www.blacklistalert.org/
- **기능**: 
  - 실시간 블랙리스트 모니터링
  - 알림 서비스 (유료)
- **확인 항목**: 지속적인 모니터링 가능

---

## 🔄 5. 정기 모니터링 체크리스트

### 매일 확인 (자동화 권장)
- [ ] VirusTotal 스캔 결과
- [ ] Google Safe Browsing 상태
- [ ] DNS 전 세계 해석 상태

### 주간 확인
- [ ] URLVoid 블랙리스트 체크
- [ ] Sucuri SiteCheck 결과
- [ ] SSL/TLS 인증서 만료일 확인

### 월간 확인
- [ ] 종합 블랙리스트 체크 (MXToolbox)
- [ ] 보안 헤더 점검 (securityheaders.com)
- [ ] 고객사 피드백 수집 (차단 사례)

---

## 🚨 6. 차단 발견 시 대응 절차

### 즉시 조치
1. **차단 사유 확인**
   - 어떤 벤더에서 차단되었는지 확인
   - 차단 사유 카테고리 확인 (신규 도메인, 악성 사이트 등)

2. **자료 준비**
   - 서비스 설명
   - 보안 인증 정보 (ISO/IEC 27001:2022)
   - 개인정보처리방침 링크
   - HTTPS/TLS 확인 자료

3. **요청 제출**
   - 해당 벤더의 재분류/Allowlist 요청
   - 또는 고객사 IT 팀에 요청 전달

### 재분류 요청 템플릿
```
[도메인 재분류 요청]

도메인: eventflow.kr
현재 분류: [차단 카테고리]
요청 분류: 업무용 정상 사이트

서비스 정보:
- 서비스명: EventFlow
- 운영사: 주식회사 유스튜디오 (U-STUDIO CO., LTD.)
- 서비스 유형: B2B2C 멀티테넌시 웨비나 플랫폼
- 개인정보처리방침: https://eventflow.kr/privacy

보안 확인:
- HTTPS/TLS 정상 작동
- ISO/IEC 27001:2022 인증 보유
- 데이터 암호화 (전송/저장)

요청 사항:
[벤더명]에서 업무용 정상 사이트로 재분류 요청
```

---

## 🤖 7. 자동 모니터링 설정 (선택사항)

### 7.1 UptimeRobot
- **URL**: https://uptimerobot.com/
- **기능**: 
  - 웹사이트 가용성 모니터링
  - HTTP 상태 코드 확인
  - 알림 설정
- **용도**: 차단으로 인한 접속 불가 감지

### 7.2 Pingdom
- **URL**: https://www.pingdom.com/
- **기능**: 
  - 전 세계 여러 지역에서 접속 테스트
  - 성능 모니터링
  - 알림 설정
- **용도**: 특정 지역/네트워크에서의 차단 감지

### 7.3 StatusCake
- **URL**: https://www.statuscake.com/
- **기능**: 
  - 웹사이트 모니터링
  - SSL 인증서 만료 알림
  - 다중 지역 확인
- **용도**: 종합적인 모니터링

---

## 📝 8. 모니터링 스크립트 예시

### PowerShell 스크립트 (Windows)
```powershell
# 도메인 차단 체크 스크립트
$domain = "eventflow.kr"

Write-Host "=== 도메인 차단 체크 ===" -ForegroundColor Green
Write-Host "도메인: $domain`n"

# DNS 확인
Write-Host "1. DNS 확인..." -ForegroundColor Yellow
try {
    $dns = Resolve-DnsName $domain -Type A
    Write-Host "   ✅ DNS 정상: $($dns[0].IPAddress)" -ForegroundColor Green
} catch {
    Write-Host "   ❌ DNS 오류: $_" -ForegroundColor Red
}

# HTTPS 접속 확인
Write-Host "`n2. HTTPS 접속 확인..." -ForegroundColor Yellow
try {
    $response = Invoke-WebRequest -Uri "https://$domain" -Method Head -TimeoutSec 10
    Write-Host "   ✅ HTTPS 정상: $($response.StatusCode)" -ForegroundColor Green
} catch {
    Write-Host "   ❌ HTTPS 오류: $_" -ForegroundColor Red
}

# SSL 인증서 확인
Write-Host "`n3. SSL 인증서 확인..." -ForegroundColor Yellow
try {
    $tcpClient = New-Object System.Net.Sockets.TcpClient
    $tcpClient.Connect($domain, 443)
    $sslStream = New-Object System.Net.Security.SslStream($tcpClient.GetStream())
    $sslStream.AuthenticateAsClient($domain)
    $cert = $sslStream.RemoteCertificate
    $certObj = [System.Security.Cryptography.X509Certificates.X509Certificate2]$cert
    Write-Host "   ✅ 인증서 발급자: $($certObj.Issuer)" -ForegroundColor Green
    Write-Host "   ✅ 만료일: $($certObj.NotAfter)" -ForegroundColor Green
    $sslStream.Close()
    $tcpClient.Close()
} catch {
    Write-Host "   ❌ SSL 오류: $_" -ForegroundColor Red
}

Write-Host "`n=== 체크 완료 ===" -ForegroundColor Green
Write-Host "`n추가 확인:"
Write-Host "- VirusTotal: https://www.virustotal.com/gui/domain/$domain"
Write-Host "- URLVoid: https://www.urlvoid.com/scan/$domain"
Write-Host "- Google Safe Browsing: https://transparencyreport.google.com/safe-browsing/search?url=$domain"
```

### Node.js 스크립트 (크로스 플랫폼)
```javascript
// check-domain-blocking.js
const https = require('https');
const dns = require('dns').promises;

const domain = 'eventflow.kr';

async function checkDNS() {
  try {
    const addresses = await dns.resolve4(domain);
    console.log('✅ DNS 정상:', addresses);
    return true;
  } catch (error) {
    console.log('❌ DNS 오류:', error.message);
    return false;
  }
}

function checkHTTPS() {
  return new Promise((resolve) => {
    const options = {
      hostname: domain,
      port: 443,
      path: '/',
      method: 'HEAD',
      timeout: 10000,
    };

    const req = https.request(options, (res) => {
      console.log('✅ HTTPS 정상:', res.statusCode);
      resolve(true);
    });

    req.on('error', (error) => {
      console.log('❌ HTTPS 오류:', error.message);
      resolve(false);
    });

    req.on('timeout', () => {
      req.destroy();
      console.log('❌ HTTPS 타임아웃');
      resolve(false);
    });

    req.end();
  });
}

async function checkSSL() {
  return new Promise((resolve) => {
    const options = {
      hostname: domain,
      port: 443,
      method: 'GET',
    };

    const req = https.request(options, (res) => {
      const cert = res.socket.getPeerCertificate();
      if (cert) {
        console.log('✅ 인증서 발급자:', cert.issuer.CN);
        console.log('✅ 만료일:', cert.valid_to);
        resolve(true);
      } else {
        console.log('❌ 인증서 정보 없음');
        resolve(false);
      }
    });

    req.on('error', (error) => {
      console.log('❌ SSL 오류:', error.message);
      resolve(false);
    });

    req.end();
  });
}

async function main() {
  console.log('=== 도메인 차단 체크 ===\n');
  console.log(`도메인: ${domain}\n`);

  console.log('1. DNS 확인...');
  await checkDNS();

  console.log('\n2. HTTPS 접속 확인...');
  await checkHTTPS();

  console.log('\n3. SSL 인증서 확인...');
  await checkSSL();

  console.log('\n=== 체크 완료 ===');
  console.log('\n추가 확인:');
  console.log(`- VirusTotal: https://www.virustotal.com/gui/domain/${domain}`);
  console.log(`- URLVoid: https://www.urlvoid.com/scan/${domain}`);
  console.log(`- Google Safe Browsing: https://transparencyreport.google.com/safe-browsing/search?url=${domain}`);
}

main();
```

---

## 📞 9. 고객사 피드백 수집

### 차단 사례 수집 방법
1. **고객사 문의 시 차단 정보 수집**
   - 어떤 보안 솔루션에서 차단되었는지
   - 차단 메시지 내용
   - 차단 카테고리

2. **차단 사례 데이터베이스 구축**
   - 벤더별 차단 빈도
   - 차단 사유 분석
   - 대응 이력

3. **프로액티브 대응**
   - 자주 차단되는 벤더에 사전 요청
   - 신규 고객사 온보딩 시 IT 팀에 사전 안내

---

## ✅ 10. 빠른 체크 리스트

### 일일 체크 (5분)
- [ ] VirusTotal: https://www.virustotal.com/gui/domain/eventflow.kr
- [ ] Google Safe Browsing: https://transparencyreport.google.com/safe-browsing/search?url=eventflow.kr

### 주간 체크 (10분)
- [ ] URLVoid: https://www.urlvoid.com/scan/eventflow.kr
- [ ] Sucuri SiteCheck: https://sitecheck.sucuri.net/
- [ ] DNS Checker: https://dnschecker.org/#A/eventflow.kr

### 월간 체크 (30분)
- [ ] MXToolbox 블랙리스트: https://mxtoolbox.com/SuperTool.aspx?action=blacklist%3aeventflow.kr
- [ ] SSL Labs: https://www.ssllabs.com/ssltest/analyze.html?d=eventflow.kr
- [ ] Security Headers: https://securityheaders.com/?q=https://eventflow.kr

---

## 🔗 11. 유용한 링크 모음

### 도메인 평판 체크
- VirusTotal: https://www.virustotal.com/
- URLVoid: https://www.urlvoid.com/
- Sucuri SiteCheck: https://sitecheck.sucuri.net/
- Google Safe Browsing: https://transparencyreport.google.com/safe-browsing/search

### DNS 확인
- DNS Checker: https://dnschecker.org/
- MXToolbox: https://mxtoolbox.com/

### SSL/TLS 확인
- SSL Labs: https://www.ssllabs.com/ssltest/
- SSL Checker: https://www.sslshopper.com/ssl-checker.html

### 보안 헤더
- Security Headers: https://securityheaders.com/

### 종합 모니터링
- UptimeRobot: https://uptimerobot.com/
- Pingdom: https://www.pingdom.com/
- StatusCake: https://www.statuscake.com/

---

**문서 버전**: v1.0  
**최종 업데이트**: 2026-02-03
