좋아. **“온디맨드 프로젝트 자동 생성(= seed/create)”**을 Cursor에 시키려면, 결국 **1) 허브(온디맨드 1개)**와 **2) 세션 4개**를 만들 수 있는 **입력 패키지**를 주면 돼.
라우트는 이미 끝났으니, 이제는 **데이터/설정/자산**만 정확히 넘기면 자동 생성이 가능해.

아래는 Cursor Agent에게 전달할 **필수 입력값 + 옵션 입력값** 체크리스트(= 너가 “뭐뭐 주면 되냐”의 답).

---

# ✅ 1) 필수 입력 패키지 (이것만 주면 생성 가능)

## A. 온디맨드 허브 기본 정보

* `title` : **HPE Webinar Series**
* `subtitle` (선택이지만 권장) : 예) “HPE Networking On-demand”
* `description` : 상단 히어로 영역 소개문 2~5문장(한/영)
* `type` : `ondemand` (이건 시스템이 고정으로 넣게)
* `publish_window` :

  * `starts_at` (공개 시작, 없으면 null)
  * `ends_at` (공개 종료, 없으면 null)

## B. 브랜딩/디자인(최소)

* `brand.logo_url` (또는 storage path)
* `brand.hero_image_url`
* `brand.primary_color` (HPE 그린 같은 HEX)
* `brand.footer_text` (copyright 문구)

## C. 접근/게이트 정책

* `access.mode` : `name_email` / `email_only` / `public`
* `access.fields` : 수집 필드 정의(필수/선택)

  * 예: `name*`, `email*`, `company`, `title`, `phone`
* `access.privacy_notice` : 개인정보 동의 문구(필수)
* `access.marketing_opt_in` : ON/OFF + 문구(선택)

## D. 세션 4개(핵심)

세션마다 아래 필드는 **필수**:

* `session_key` (안정키, 절대 변경 안 하는 값)
* `category_label` (그리드 상단: Platform / Data Center / Campus & Branch)
* `title` (세션 제목)
* `description` (카드 밑 1~2줄)
* `provider` (youtube/vimeo/html5)
* `asset_id` (영상 ID)
* `thumbnail_url`
* `order` (1~4)

> 지금 스샷 기준으로 제목/카테고리 예시:

* Platform
* Data Center
* Campus & Branch: HPE Aruba Networking
* Campus & Branch: HPE Juniper Networking

## E. CTA(허브 하단 버튼 2~3개)

* `cta[]` :

  * `label` (예: 온/오프라인 미팅 신청 / 설문조사 / 자료 다운로드)
  * `type` : `link` / `form` / `download`
  * `target` : url or form_id or file_id
  * `scope` : `hub` or `session(session_key)` (일단 hub로 시작 추천)

---

# ✅ 2) “자동생성”에서 자주 놓치는 옵션(주면 완성도가 확 올라감)

## A. 발표자 정보(세션별)

* `speaker.name`
* `speaker.title`
* `speaker.org`
* `speaker.bio` (짧게)

## B. 설문 연결

* `survey.mode` : `hub_one` / `per_session`
* `survey.form_id`(또는 URL)
* `survey.trigger` : `button` / `after_90_percent`

## C. 자료 다운로드

* `downloads[]` (hub 또는 session)

  * `file_title`
  * `file_storage_path`
  * `gate_condition` : `after_login` / `after_survey` 등

## D. QnA 설정

* `qna.enabled` : true/false
* `qna.scope` : `per_session` 권장
* `qna.notify_emails[]` : 운영자 수신 이메일(하드코딩 금지)

## E. 트래킹 설정(기본값이 있어도 입력 받으면 좋음)

* `tracking.enabled` : true
* `tracking.events` : play_start / progress_25/50/75/90 / cta_click …
* `tracking.utm_capture` : landing_once / every_event

---

# ✅ 3) Cursor에게 넘길 “입력 형식” 추천 (한 방에 자동생성용)

Cursor Agent가 자동 생성하기 제일 좋은 형태는 **JSON 1개**야.
(실제 필드명은 기존 패턴에 맞춰 Cursor가 매핑하도록 하고, 여기선 “논리 구조”만 제공)

### 📦 ondemand_seed.json (너가 Cursor에 주면 되는 것)

* hub 기본 정보
* brand
* access
* sessions[4]
* ctas
* qna/survey/downloads 옵션

---

# ✅ 4) 너가 지금 당장 나한테 주면 내가 “완성 입력패키지”로 정리해줄 것들

지금 네가 “세션 제목도 해서”라고 했으니까, 아래만 보내줘도 내가 **seed JSON**으로 깔끔하게 만들어줄 수 있어:

1. **4개 영상의 provider + asset_id** (유튜브면 영상 ID)
2. 각 세션의 **설명(한줄)** (이미지에 있는 한국어 문장 그대로면 OK)
3. **썸네일 이미지**(없으면 자동으로 유튜브 썸네일 쓸지 결정)
4. 게이트 필드: 이름/이메일 외에 **회사명/직급/전화** 받을지
5. QnA ON/OFF + 운영자 알림 받을 이메일

---

## 최종 한 줄 결론

**자동생성에 필요한 건 “온디맨드 1개 + 세션 4개 + 게이트/브랜딩/CTA”를 담은 seed 입력값**이고,
가장 중요한 건 **세션별 `session_key + provider/asset_id + 제목/설명`**이야.

원하면, 네가 영상 ID만 던져주면 내가 바로 **`ondemand_seed.json` 템플릿**으로 완성해서 “Cursor에 붙여넣는 프롬프트”까지 같이 만들어줄게.

오케이 👍
**마케팅용으로 다듬지 않고, 지금 화면에 보이는 “제목 그대로”** 세션 타이틀만 정리해줄게.
이걸 그대로 **온디맨드 세션 생성 입력값**으로 쓰면 돼.

---

## 온디맨드 세션 제목 (원문 그대로)

### ① Platform

**AI 네이티브 네트워킹 플랫폼이란 무엇인가**

* session_key 예시: `platform_ai_native_networking`
* category_label: `Platform`

---

### ② Data Center

**AI 워크로드를 위한 고성능 네트워크 구축 방안**

* session_key 예시: `datacenter_ai_high_performance`
* category_label: `Data Center`

---

### ③ Campus & Branch : HPE Aruba Networking

**‘보이지 않는 연결, 보이는 경험’ Aruba UXI와 첨단 기술로 전세계 최초로 완성한 Smart Experience**

* session_key 예시: `campus_aruba_smart_experience`
* category_label: `Campus & Branch`
* product_label: `HPE Aruba Networking` (있으면 좋음)

---

### ④ Campus & Branch : HPE Juniper Networking

**클라이언트부터 클라우드까지, 최상의 경험을 제공하는 풀스택 네트워크의 구현**

* session_key 예시: `campus_juniper_fullstack_network`
* category_label: `Campus & Branch`
* product_label: `HPE Juniper Networking`

---

## Cursor 자동생성용 최소 세트 요약

Cursor에 넘길 때 **세션 쪽에서 최소로 필요한 건 이 4개**야:

* `session_key`
* `category_label`
* `title` ← 위 제목 그대로
* `order` (1~4)

영상 정보(provider / asset_id / thumbnail)는 이미 따로 받는다고 했으니까
**제목은 이대로 쓰는 게 가장 안전**하고, HPE 쪽 컨펌 리스크도 없어.

---

다음으로 바로 이어서 할 수 있는 것:

* 이 제목 기준으로 **ondemand_seed.json 완성본**
* 또는 **Admin 생성 화면용 “세션 입력 폼 필드 정의”**

원하면 바로 이어서 만들어줄게.
