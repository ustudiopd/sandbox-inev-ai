아래는 **Phase 2 UI(링크 생성/관리 화면)** 기준으로, **AI 없이(=규칙/템플릿/검증만으로)**

* 템플릿 자동 채움
* `cid` 생성 규칙(6자리/8자리/slug)
* 저장/집계 방식(전환/유입/대시보드)

을 **완전히 고정**한 “실행용 명세서”입니다. Cursor Agent가 그대로 구현/리팩토링 기준으로 쓰면 됩니다.

---

# 광고/캠페인 링크(UTM + cid) 명세서 v1.0

**전제:** 숏링크 기능(`short_links`, `/s`)은 사용하지 않는다. 링크 추적은 **UTM querystring + cid(query param)**만 사용한다.

## 1. 목적

운영자가 광고/공유용 링크를 만들 때 UTM을 직접 입력하지 않아도 되도록 **템플릿 기반 자동 채움**을 제공하고, 카톡/문자/QR에 적합한 **짧은 링크 식별자 `cid`**를 부여해 유입/전환을 안정적으로 집계한다.

---

## 2. 스코프와 원칙

### 2.1 스코프

* 대상: **등록/설문 이벤트 캠페인**(예: `/event/149403/register`)
* Phase 2 UI에 “캠페인 링크 생성/목록/복사” 기능이 이미 존재한다는 전제에서 UX/규칙/집계 기준을 확정한다.

### 2.2 원칙(고정)

* **UTM은 querystring으로만** 전달한다. (`utm_source` 등) 
* **cid는 querystring으로만** 전달한다. (예: `?cid=AB12CD`)
* **path 세그먼트(/event/149403/123456) 방식은 사용하지 않는다.**
* 워트처럼 `/webinar -> /event` 리다이렉트가 있어도 **querystring pass-through**로 UTM/cid 유실이 없어야 한다. 
* 멀티테넌시: 모든 링크/집계는 **client_id 스코프 고정**(Guard/RLS 패턴 준수). 

---

## 3. 핵심 용어/지표 정의

### 3.1 전환(Conversion)

* `event_survey_entries` 생성(등록/설문 제출 완료)을 전환으로 정의한다. 
* 전환 저장 필드(필수):

  * utm_* (정규화된 값)
  * `marketing_campaign_link_id` (nullable, 링크로 유입된 경우만)
  * `utm_first_visit_at`(보조), `utm_referrer`(도메인 권장)

### 3.2 유입(Visit)

* Visit는 **선택 구현**이지만, CVR을 보여줄 계획이면 Phase 3로 visit 로깅 도입을 전제한다. 
* Visit 정의(고정):

  * `session_id`(익명 랜덤) 기준 dedup
  * TTL(세션 유효 시간)은 config로

### 3.3 캠페인 링크(Campaign Link)

* 운영자가 이름을 붙여 관리하는 “한 개의 배포 링크 단위”
* 이 엔티티는 **UTM 세트 + cid + target(캠페인/랜딩)**를 한 묶음으로 가진다.

---

## 4. 데이터 모델(고정)

### 4.1 marketing_campaign_links 테이블(필수)

**목적:** 숏링크 없이도 “운영 링크 단위”를 저장/집계하는 단일 진실(Single Source of Truth)

필드(개념) — 실제 컬럼명은 컨벤션에 맞추되 의미는 동일해야 함:

* `id` (uuid, PK)  ← **전환/로그에서 참조할 단일 진실**
* `client_id`, `agency_id` (스코프)
* `target_type` (enum: `event_campaign`)
* `target_id` (campaign_id)  ※ 전환 타겟 캠페인
* `landing_variant` (enum: `welcome | register | survey | done`)
* `cid` (text, unique within client_id)  ← 6/8/slug 중 선택, 아래 규칙 참고
* `name` (운영자 이름: “26년 1월 뉴스레터 2차”)
* `utm_source`, `utm_medium`, `utm_campaign`, `utm_term`, `utm_content`
* `is_active`(또는 status: active/paused/archived)
* `created_by`, `created_at`, `updated_at`

인덱스(최소):

* unique(`client_id`, `cid`)
* (`client_id`, `target_id`, `is_active`)
* (`client_id`, `utm_source`, `utm_medium`, `utm_campaign`) (집계가 느릴 때)

> “utm_campaign 한글 풀문장”은 금지. 운영자 이름(name)에 넣고, utm_campaign은 slug 규칙으로 생성한다. 

---

### 4.2 event_survey_entries 확장(Phase 1~2에서 이미 진행된 전제)

* `utm_*` 컬럼(방안 A) + `utm_first_visit_at`, `utm_referrer`
* `marketing_campaign_link_id` (nullable)

  * URL에 `cid`가 있으면 이를 lookup하여 링크 id를 저장

(UTM 저장 접근은 기존 보고서 방향 유지) 

---

## 5. 템플릿 자동 채움 UX 명세(Phase 2 UI)

### 5.1 UI 구성(링크 생성 폼)

필드 구성은 아래 순서를 고정한다.

1. **전환 타겟 선택(필수)**

* 등록/설문 캠페인 선택
* 선택 시 기본 랜딩 variant 자동 추천(대부분 `register`)

2. **템플릿 선택(필수)**

* 버튼/드롭다운:

  * 📧 뉴스레터
  * 💬 카카오(공유/채널)
  * 📱 문자(SMS)
  * 🔍 구글 광고
  * 📘 메타 광고
  * 🤝 파트너/제휴
  * 🧪 기타(커스텀)

3. **링크 이름(name) (필수)**

* 운영자가 알아볼 이름
* 예: “26년 1월 뉴스레터 2차”, “카톡 공유”, “구글검색_브랜드”

4. **UTM 자동 생성 영역(기본은 잠금, ‘고급 옵션’에서만 편집)**

* template 선택과 name 입력을 기반으로 시스템이 생성/세팅:

  * utm_source
  * utm_medium
  * utm_campaign (slug)
  * utm_content (옵션)
  * utm_term (광고 템플릿에서만 옵션)

5. **cid 생성(필수, 자동)**

* 생성 시점에 cid를 자동 부여 (아래 규칙)

6. **생성 결과**

* “공유용 링크(짧음)”

  * `https://eventflow.kr/event/{public_path}/{variant}?cid=XXXXXX`
* “광고용 링크(표준 UTM 포함)”

  * `https://eventflow.kr/event/{public_path}/{variant}?cid=XXXXXX&utm_source=...&utm_medium=...&utm_campaign=...`

> 공유용 링크도 cid만 달고 끝내지 말고, 내부적으로는 cid로 UTM을 역추적할 수 있어야 한다(집계/디버깅). 

---

### 5.2 템플릿별 기본값(고정)

템플릿이 채우는 기본값은 아래를 기본 세트로 하고, 드롭다운 사전(Dictionary)로 관리 가능하게 한다(하드코딩이 아니라 “기본값 테이블/설정”으로).

| Template | utm_source 기본 | utm_medium 기본 | utm_term | 비고                       |
| -------- | ------------- | ------------- | -------- | ------------------------ |
| 뉴스레터     | newsletter    | email         | 비움       | content로 회차/위치 구분 권장     |
| 카카오      | kakao         | social        | 비움       | 카카오 채널/공유 구분은 content    |
| 문자       | sms           | sms           | 비움       | reminder 여부는 content     |
| 구글 광고    | google        | cpc           | 옵션       | term은 키워드일 때만            |
| 메타 광고    | facebook      | cpc           | 비움       | 인스타는 source=instagram 옵션 |
| 파트너      | partner       | referral      | 비움       | 파트너명은 content 권장         |
| 기타       | (직접 선택)       | (직접 선택)       | 옵션       | 자유                       |

---

### 5.3 utm_campaign 자동 생성 규칙(고정)

한글 문장 금지. 영문/숫자/언더스코어만 허용.

**규칙(권장 포맷)**

```
{client_slug}_{target_slug}_{yyyymm}_{channel}_{seq?}
```

* client_slug: client name을 slugify(예: wert)
* target_slug: 전환 타겟의 slug(예: patent_webinar)
* yyyymm: 생성 시점 또는 운영자가 선택한 월
* channel: template에서 결정(newsletter/sms/kakao/google/facebook/partner)
* seq: “1차/2차” 등 반복 발송이면 01/02…

예시:

* `wert_patent_webinar_202601_newsletter_02`
* `wert_patent_webinar_202601_kakao_share`

**slugify 규칙**

* 공백/특수문자 제거
* 한글은 로마자 변환까지는 필수 아님(복잡) → 기본은 제거/치환
  (현실적으로는 target_slug/client_slug는 시스템에서 미리 정해진 값 사용 권장)

---

### 5.4 utm_content 자동/옵션 규칙(고정)

기본 비움. 아래 상황에서만 활성화:

* 뉴스레터: `nl_01`, `nl_02`, `footer`, `hero`, `cta_a`
* 카카오: `share`, `channel_post`, `openchat`
* 문자: `reminder`, `lastcall`
* 구글/메타: `creative_a`, `creative_b`, `placement_feed`, `placement_story`
* 파트너: `partner_{name}`

---

## 6. cid 생성 규칙(6자리/8자리/slug) 확정

### 6.1 선택 전략(권장)

* 기본: **8자리 Base32/Alnum** (충돌/추측 리스크 감소)
* 옵션: 6자리(간편) / slug(가독)

**권장 결론**

* **Default = 8자리**
* 6자리는 “오프라인 수기 입력용” 같은 특수 목적일 때만
* slug형은 “사람이 읽는 링크”가 필요할 때만(충돌 관리가 어려움)

### 6.2 생성 규칙(고정)

* `cid`는 `marketing_campaign_links` 생성 시 서버에서 발급(클라이언트 생성 금지)
* 유일성 스코프: `(client_id, cid)` unique
* 허용 문자:

  * 6/8자리: `[A-Z0-9]` (대문자) 추천
  * slug: `[a-z0-9-]`

**표현 예**

* 8자리: `AB12CD34`
* 6자리: `A1B2C3`
* slug: `wert-nl-202601-02` (권장하지 않음)

---

## 7. 링크 URL 생성 규칙(고정)

### 7.1 base URL(도메인 통일)

* 항상 `https://eventflow.kr` 사용 (must.ai.kr 사용 금지)

### 7.2 링크 형태(variant 포함)

* target이 캠페인이므로 기본은:

  * `https://eventflow.kr/event/{public_path}/{variant}`
    예: `/event/149403/register`

### 7.3 querystring 구성(순서 고정 권장)

공유용(짧음):

```
...?cid=AB12CD34
```

광고용(표준):

```
...?cid=AB12CD34&utm_source=...&utm_medium=...&utm_campaign=...&utm_content=...
```

> cid는 항상 포함(권장). 그래야 공유/광고 모두 링크 단위로 묶여 운영이 쉬움.

---

## 8. 저장/집계 방식(고정)

### 8.1 전환 저장(필수)

* 공개 submit/register API에서:

  * querystring에서 `cid` 수신
  * `marketing_campaign_links`를 `(client_id, cid)`로 lookup
  * 성공 시 `marketing_campaign_link_id`를 `event_survey_entries`에 저장
  * UTM도 함께 저장(정규화 필수)

### 8.2 UTM 정규화 규칙(고정)

* `utm_*` 저장 시:

  * trim
  * lowercase (단, cid는 대문자 유지)
  * 길이 제한(예: 200)
* utm 없음(null)은 “Direct (UTM 없음)”으로 집계/표시 

### 8.3 유입(Visit) 저장(Phase 3 옵션)

* Visit endpoint에서:

  * `cid` 기반 link lookup 후 `marketing_campaign_link_id` 저장
  * session_id 기반 dedup
  * IP/UA 기본 미수집

### 8.4 대시보드 집계(Phase 2+)

* Phase 2(현재): conversions 중심

  * by link(name/cid)
  * by utm_source/medium/campaign
  * by target_campaign
* 집계는 DB group-by/RPC 권장(루프 집계 금지)

---

## 9. WERT(149403↔149402) 유실 방지 규칙(고정)

워트 구조처럼 `/webinar/{id}`에서 등록 없으면 `/event/{public_path}`로 보내는 경우, **cid/utm querystring pass-through**가 필수다. 

규칙:

* 모든 redirect(서버/클라)는 현재 URL의 `cid` 및 `utm_*`를 다음 URL에 유지

---

## 10. 운영 정책(권장)

* **중복 생성 방지**: 동일 target + 동일 template + 동일 seq로 생성 시 경고
* **상태 관리**: paused면 링크 생성/복사는 가능하되 “운영 중단” 표기 (전환 기록은 남을 수 있음)
* **변경 이력**: name/utm 변경은 audit 로그 또는 updated_at 기록(선택)

---

## 11. Definition of Done (DoD)

### 생성/복사 DoD

* [ ] 템플릿 선택만으로 utm_source/utm_medium 자동 세팅
* [ ] name 입력으로 utm_campaign 자동 생성(slug)
* [ ] 링크 생성 시 cid 자동 발급(6/8/slug 정책 적용)
* [ ] 공유용/광고용 URL이 동시에 출력

### 추적/집계 DoD

* [ ] submit/register 시 cid로 link lookup → `marketing_campaign_link_id`가 entries에 저장
* [ ] 대시보드에서 link(name/cid)별 conversions 집계가 정확히 표시
* [ ] UTM 없음은 “Direct(UTM 없음)”으로 통일

---

# Cursor Agent 착수 메모(중요)

* 이 명세는 “숏링크 없음” 전제이므로 `short_links` 수정/확장은 하지 않는다.
* `cid`는 **path가 아니라 querystring**으로만 운영한다.
* `utm_campaign`에 한글 긴 문장을 넣는 운영은 금지하고, name 필드로 분리한다.

---

원하시면, 다음으로 **“템플릿/사전(Dictionary) 설정을 DB로 둘지, 코드 상수로 둘지”**를 결정해드릴게요.
(프로젝트 원칙상 ‘하드코딩 금지’에 가깝게 가려면 DB 테이블로 빼는 게 맞고, 초기엔 코드로 두되 운영 화면에서 관리 가능한 구조로 확장하는 절충안도 가능합니다.)
