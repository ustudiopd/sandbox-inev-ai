# 슬래시 메뉴 적용 실패 현황 분석 명세서

**작성 일자**: 2025-01-22  
**프로젝트**: EventLive  
**문제**: 슬래시(`/`) 메뉴 명령은 적용되어 DOM이 변경되지만, Tailwind Preflight로 인해 heading/list가 unstyled라 UI 변화가 보이지 않음  
**상태**: ✅ 해결됨 (Tailwind Typography `prose` 적용)

---

## 📋 현재 구현 상태

### 1. 코드 구조

#### `components/editor/extensions.tsx`
```typescript
// ✅ renderItems import 완료
import { Command, createSuggestionItems, renderItems } from 'novel'

// ✅ suggestionItems 정의 완료
export const suggestionItems = createSuggestionItems([...])

// ✅ slashCommand export 완료 (하이브리드 방식)
export const slashCommand = Command.configure({
  suggestion: {
    items: () => suggestionItems,
    render: renderItems, // ← 설정됨
  },
})
```

#### `components/editor/BlogEditor.tsx`
```typescript
// ✅ slashCommand import 및 사용
import { suggestionItems, slashCommand } from './extensions'

// ✅ extensions 배열에 포함
const extensions = [
  // ... 기타 확장들 ...
  slashCommand,
]

// ✅ EditorCommand 수동 렌더링
<EditorCommand>
  <EditorCommandList>
    {suggestionItems.map((item) => (
      <EditorCommandItem
        onCommand={({ editor, range }) => {
          if (item.command) {
            item.command({ editor, range })
          }
        }}
        // ...
      />
    ))}
  </EditorCommandList>
</EditorCommand>

// ✅ handleCommandNavigation 연결
editorProps={{
  handleDOMEvents: {
    keydown: (_view, event) => handleCommandNavigation(event) || false
  }
}}

// ✅ SSR 설정
<EditorContent immediatelyRender={false} />
```

---

## 🔍 문제 증상

### 관찰된 현상

1. **슬래시 메뉴 표시**: ✅ 정상 작동
   - `/` 입력 시 메뉴가 정상적으로 표시됨
   - 필터링도 정상 작동

2. **명령 실행 로그**: ✅ 콘솔에 출력됨
   ```
   Command triggered: {title: 'Heading 1', hasEditor: true, hasRange: true}
   Command executed: {title: 'Heading 1', contentChanged: true, afterContent: {...}}
   ```

3. **DOM 변경**: ✅ **정상 작동**
   - 콘솔 로그: `DOM changed: true`
   - `<h1>`, `<ul>` 등 태그가 정상적으로 생성됨
   - Before: `<p>/안녕</p>` → After: `<h1>안녕</h1>`

4. **에디터 UI 스타일**: ❌ **스타일이 적용되지 않아 변화가 보이지 않음**
   - DOM은 변경되었지만 CSS가 없어서 `<h1>`도 일반 텍스트처럼 보임
   - `fontSize: '16px'`, `fontWeight: '400'`, `marginTop: '0px'` (기본값)
   - `First heading classes: ''` (클래스 미적용)

---

## 🧪 디버깅 결과

### 콘솔 로그 분석

```javascript
// 명령 호출 확인
Command triggered: {
  title: 'Heading 1',
  hasEditor: true,    // ✅ editor 존재
  hasRange: true      // ✅ range 존재
}

// 명령 실행 확인
Command executed: {
  title: 'Heading 1',
  contentChanged: true,  // ✅ 내용 변경됨
  afterContent: {...}     // ✅ 변경된 내용 확인됨
}
```

### 분석 결과

- `onCommand` 핸들러는 정상 호출됨 ✅
- `editor`와 `range`는 정상적으로 전달됨 ✅
- `item.command` 함수는 실행됨 ✅
- 에디터의 JSON 내용은 변경됨 ✅
- **DOM은 변경됨** (`<h1>`, `<ul>` 등 태그 생성) ✅
- **하지만 스타일이 적용되지 않아 UI 변화가 보이지 않음** ❌
  - Tailwind Preflight가 heading/list 기본 스타일 제거
  - `@tailwindcss/typography` 플러그인 없음 → `prose` 클래스 미작동

---

## 🔍 가능한 원인 분석

### 원인 1: `renderItems`와 수동 렌더링 충돌

**가설**: `renderItems`가 자동 렌더링을 시도하는데, 수동으로 `suggestionItems.map()`을 사용하면서 충돌 발생

**증거**:
- 가이드 문서에서는 하이브리드 방식을 권장하지만, 실제 동작 방식이 명확하지 않음
- `renderItems`가 내부적으로 자체 렌더링을 수행할 수 있음

**확인 필요**:
- `renderItems`가 실제로 무엇을 하는지
- 수동 렌더링과 자동 렌더링이 충돌하는지

### 원인 2: `EditorCommand` 위치 문제

**가설**: `EditorCommand`가 `EditorContent` 내부에 있어야 하는데, 현재 위치가 잘못되었을 수 있음

**현재 구조**:
```tsx
<EditorContent>
  <EditorBubbleWrapper />
  <ImageUploadHandler />
  <div ref={commandMenuRef}>
    <EditorCommand>
      {/* 수동 렌더링 */}
    </EditorCommand>
  </div>
</EditorContent>
```

**확인 필요**:
- `EditorCommand`가 `EditorContent`의 직접 자식이어야 하는지
- `commandMenuRef` div 래퍼가 문제를 일으키는지

### 원인 3: `items` 함수와 수동 렌더링 불일치

**가설**: `slashCommand`의 `items` 함수가 `suggestionItems`를 반환하는데, 수동으로 `suggestionItems.map()`을 사용하면서 Novel의 내부 상태와 불일치

**현재 코드**:
```typescript
// extensions.tsx
items: () => suggestionItems

// BlogEditor.tsx
{suggestionItems.map((item) => ...)}
```

**확인 필요**:
- `items` 함수가 반환하는 아이템과 수동 렌더링하는 아이템이 동일한지
- Novel이 내부적으로 필터링한 아이템과 수동 렌더링 아이템이 동기화되는지

### 원인 4: 에디터 인스턴스 동기화 문제

**가설**: `onCommand`에서 받은 `editor`와 실제 에디터 인스턴스가 다를 수 있음

**확인 필요**:
- `onCommand`의 `editor`가 실제 에디터 인스턴스와 동일한지
- 에디터 상태 업데이트가 제대로 전파되는지

### 원인 5: `range` 처리 문제

**가설**: `range`가 있지만 실제로 `deleteRange(range)`가 제대로 실행되지 않을 수 있음

**현재 코드**:
```typescript
command: ({ editor, range }) => {
  if (range) {
    editor.chain().focus().deleteRange(range).setNode('heading', { level: 1 }).run()
  }
}
```

**확인 필요**:
- `range` 객체가 유효한지
- `deleteRange(range)`가 실제로 실행되는지
- 에러가 발생하지만 조용히 실패하는지

---

## ✅ 최종 해결 방법: Tailwind Typography 적용

### 적용된 수정사항

#### 1. `@tailwindcss/typography` 플러그인 설치 및 활성화

**package.json**:
```json
"devDependencies": {
  "@tailwindcss/typography": "^0.5.x"
}
```

**tailwind.config.ts**:
```typescript
plugins: [
  require('@tailwindcss/typography'),
],
```

#### 2. 에디터 콘텐츠 영역에 `prose` 클래스 적용

**BlogEditor.tsx** - `editorProps.attributes.class`:
```typescript
class: 'prose prose-slate max-w-none dark:prose-invert focus:outline-none min-h-[350px] ...'
```

**핵심 클래스 설명**:
- `prose`: 기본 타이포그래피 스타일 적용
- `prose-slate`: slate 색상 테마
- `max-w-none`: prose 기본 max-width 제한 제거
- `dark:prose-invert`: 다크모드 지원

#### 3. Slash 메뉴에 `not-prose` 클래스 추가

**BlogEditor.tsx** - `EditorCommand`:
```typescript
<EditorCommand className="not-prose z-50 ...">
```

**목적**: Slash 메뉴가 prose 타이포 스타일의 영향을 받지 않도록 제외

#### 4. Editor 인스턴스 안정화 및 외부 상태 원복 방지

- `editorKey={Date.now()}` 제거
- `initialContentRef`를 사용하여 초기값만 유지

---

## 🔧 시도한 해결 방법 (이전)

### 시도 1: `renderItems` 직접 사용
- **결과**: `render is not a function` 에러 발생
- **원인**: `renderItems(commandMenuRef)` 형식이 잘못됨
- **해결**: `renderItems`를 직접 함수로 사용

### 시도 2: `commandMenuRef` 제거
- **결과**: 여전히 적용되지 않음
- **원인**: `commandMenuRef`는 문제가 아니었음

### 시도 3: `onCommand` 형식 변경
- **시도**: `({ editor, range }) => ...` 형식으로 변경
- **결과**: 콘솔 로그는 정상이지만 여전히 적용되지 않음

### 시도 4: 가이드 문서에 따른 하이브리드 방식 적용
- **변경사항**:
  - `extensions.tsx`에 `slashCommand` export 추가
  - `items: () => suggestionItems` 단순화
  - `render: renderItems` 설정
- **결과**: 여전히 적용되지 않음

---

## 📊 최종 코드 상태 체크리스트

### ✅ 완료된 항목

**슬래시 메뉴 기능**:
- [x] `renderItems` import 및 설정
- [x] `slashCommand` export 및 사용
- [x] `onCommand` 형식: `({ editor, range }) => ...`
- [x] `extensions` 배열에 `slashCommand` 포함
- [x] `handleCommandNavigation` 연결
- [x] `immediatelyRender={false}` 설정
- [x] 중복 확장 해결 (`link`, `horizontalRule`, `underline`)

**스타일 적용**:
- [x] `@tailwindcss/typography` 플러그인 설치 및 활성화
- [x] 에디터 콘텐츠 영역에 `prose` 클래스 적용
- [x] Slash 메뉴에 `not-prose` 클래스 추가 (prose 영향 제외)
- [x] `max-w-none` 적용 (레이아웃 깨짐 방지)
- [x] `dark:prose-invert` 적용 (다크모드 지원)

**에디터 안정화**:
- [x] `editorKey={Date.now()}` 제거 (에디터 재생성 방지)
- [x] `initialContentRef` 사용 (외부 상태 원복 방지)

---

## 🎯 다음 단계 제안

### 단계 1: `renderItems` 제거 테스트

`renderItems`를 제거하고 완전 수동 방식으로 테스트:

```typescript
// extensions.tsx
export const slashCommand = Command.configure({
  suggestion: {
    items: () => suggestionItems,
    // render: renderItems, ← 제거
  },
})
```

```typescript
// BlogEditor.tsx
const { editor } = useCurrentEditor()

<EditorCommandItem
  onCommand={() => {
    if (!editor) return
    // range는 Command 확장에서 가져오기
    const { state } = editor.view
    const { $from } = state.selection
    const range = { from: $from.pos - 1, to: $from.pos }
    item.command?.({ editor, range })
  }}
/>
```

### 단계 2: Novel 공식 예제 확인

Novel.sh의 공식 예제를 확인하여 올바른 사용법 확인

### 단계 3: 에디터 상태 강제 업데이트

명령 실행 후 에디터 상태를 강제로 업데이트:

```typescript
onCommand={({ editor, range }) => {
  if (item.command) {
    item.command({ editor, range })
    // 강제 업데이트 시도
    setTimeout(() => {
      editor.view.dispatch(editor.state.tr)
    }, 0)
  }
}}
```

### 단계 4: 디버깅 강화

더 상세한 로그 추가:

```typescript
onCommand={({ editor, range }) => {
  console.log('=== Command Debug ===')
  console.log('Editor:', editor)
  console.log('Range:', range)
  console.log('Editor state before:', editor.getJSON())
  
  if (item.command) {
    const result = item.command({ editor, range })
    console.log('Command result:', result)
    
    setTimeout(() => {
      console.log('Editor state after:', editor.getJSON())
      console.log('Editor view state:', editor.view.state)
    }, 100)
  }
}}
```

---

## 📝 참고 자료

- `ustudio_슬래시_메뉴_수동_렌더링_개선_가이드.md`
- `블로그_에디터_이식_슬래시_메뉴_이슈_해결_보고서.md`
- `블로그_에디터_이식_슬래시_메뉴_이슈_해결_보고서_검토.md`

---

## 🔴 핵심 문제 요약

**문제**: 슬래시 메뉴 명령은 적용되어 DOM이 변경되지만, Tailwind Preflight로 인해 heading/list가 unstyled라 UI 변화가 보이지 않음

**증상**:
- 메뉴 표시: ✅ 정상
- 명령 호출: ✅ 정상 (`onCommand` 실행됨)
- 에디터 JSON 변경: ✅ 정상 (`contentChanged: true`)
- **DOM 변경**: ✅ 정상 (`<h1>`, `<ul>` 등 태그 생성 확인)
- **에디터 UI 업데이트**: ❌ **스타일이 적용되지 않아 변화가 보이지 않음**

**근본 원인**:
- **Tailwind Preflight가 heading/list 기본 스타일을 제거**
- DOM은 변경되었지만 CSS가 없어서 `<h1>`도 일반 텍스트처럼 보임
- `@tailwindcss/typography` 플러그인이 없어서 `prose` 클래스가 작동하지 않음

---

## ✅ 해결 방법: Tailwind Typography `prose` 적용

### 1. `@tailwindcss/typography` 플러그인 설치 및 활성화

**설치**:
```bash
npm install -D @tailwindcss/typography
```

**tailwind.config.ts 설정**:
```typescript
plugins: [
  require('@tailwindcss/typography'),
],
```

### 2. 에디터 콘텐츠 영역에만 `prose` 적용

**적용 위치**: `EditorContent`의 `editorProps.attributes.class`

**핵심 클래스**:
- `prose`: 기본 타이포그래피 스타일
- `max-w-none`: prose 기본 max-width 제한 제거 (레이아웃 깨짐 방지)
- `dark:prose-invert`: 다크모드 지원
- `prose-slate`: slate 색상 테마

**주의사항**:
- `prose`는 ProseMirror 콘텐츠 영역에만 적용
- Slash 메뉴(`EditorCommand`)는 `not-prose` 클래스로 prose 영향 제외

### 3. Editor 인스턴스 안정화
- `editorKey={Date.now()}` 제거 (매 렌더마다 에디터 재생성 방지)

### 4. 외부 상태 원복 방지
- `initialContentRef`를 사용하여 초기값만 유지
- `onChange`로 업데이트된 `content`가 에디터를 리셋하지 않도록 수정

---

## 📊 완료 조건 (DoD)

다음 조건을 만족하면 해결 완료:

1. `/ Heading 1` 실행 후
   - DOM: `<h1>안녕</h1>` 유지 ✅
   - computedStyle:
     - `fontSize`가 16px보다 큼 (예: 30px)
     - `fontWeight`가 400보다 큼 (예: 700)
     - `marginTop`/`marginBottom`가 0이 아님

2. `/ Bullet List` 실행 후
   - DOM: `<ul><li>` 생성 ✅
   - 화면: 불릿/들여쓰기 가시적

3. Slash 메뉴 UI가 prose 적용 전후로 깨지지 않음
   - `not-prose` 클래스로 prose 영향 제외 ✅

---

**작성 완료**: 2025-01-22  
**상태**: ✅ 해결 완료 (Tailwind Typography 적용)
